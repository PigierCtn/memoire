jdbc.driverClassName=org.apache.commons.dbcp.BasicDataSource
jdbc.url=#{ jdbc:postgresql:// + @dbUrl.getHost() + : + @dbUrl.getPort() + @dbUrl.getPath() + ?sslmode=require }
jdbc.user=#{ @dbUrl.getUserInfo().split(':')[0] }
jdbc.password=#{ @dbUrl.getUserInfo().split(':')[1] }
